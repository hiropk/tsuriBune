<div>
  <h1 class="font-bold text-4xl">ShipCaptain#dashboard</h1>
  <p>Find me in app/views/ship_captain/dashboard.html.erb</p>
</div>

<% if authenticated? %>
  <p><%= Current.user.email_address %></p>
  <%= link_to 'Sign out', session_path, data: { "turbo-method": :delete} %>
<% else %>
  <%= link_to 'Sign in', new_session_path %>
<% end %>
