<div>
  <h1 class="font-bold text-4xl">
    <%= t('ship_captain.title')%>
  </h1>
</div>

<% if authenticated? %>
  <p><%= Current.user.email_address %></p>
  <%= link_to t('dictionary.logout'), session_path, data: { "turbo-method": :delete} %>
<% else %>
  <%= link_to t('dictionary.login'), new_session_path %>
<% end %>

