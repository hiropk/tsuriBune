<div id="<%= dom_id schedule %>">
  <p>
    <%= "メニュー名：#{schedule.menu.name}" %>
  </p>

  <p>
    <%= "出船形式：#{t("menu.enum.embarkation_type.#{schedule.menu.embarkation_type}")}" %>
  </p>

  <p>
    <%= "予約状況(予約済人数/定員）：#{schedule.num_reservation} / #{schedule.menu.acceptable_num}" %>
  </p>

  <p>
    <strong class="block font-medium mb-1">Memo:</strong>
    <%= "備考: #{schedule.memo}" %>
  </p>

  <p>
    <%= "開始時刻:" + schedule.start_time.strftime( "%H:%M" ) %>
  </p>

</div>
